## To connect Python to MySQL, 
* The most common and "Pythonic" library to use is mysql-connector-python. It’s reliable and stays close to standard SQL syntax.
* Script demonstrating CRUD (Create, Read, Update, Delete) operations for your Employees table.
## Orm usd to connect SQL DB and do operations
*`pip install mysql-connector-python`